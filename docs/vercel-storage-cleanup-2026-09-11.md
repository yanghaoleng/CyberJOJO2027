# Vercel 存储清理记录 · 2026-09-11

## 已完成

- mikeywa：删除 155 条历史部署，保留 12 条有域名绑定或用于回滚的记录；发布新版本后共 13 条。
- jojodemos：删除 48 条历史部署，保留 7 条有域名绑定或用于回滚的记录；发布新版本后共 8 条。此次验证用的临时预览已删除。
- 没有删除两个线上项目或改变正式域名绑定规则。
- CyberJOJO2027 独立 Vercel 项目在前一轮已移除；Tencent 正式站未改动。

## 防止再次增长

Mikey-index：页面预加载及常规读取优先使用 `/api/schedule`。保留静态快照作为接口异常时的回退。每小时的 GitHub Actions 改为读取并检查独立日程接口，不再调用整站部署 Hook。构建及“实时接口优先 / 接口故障回退”测试通过；手动执行新的定时任务成功。

提交：`e88a35d1c40cd1df1dd3d951c6960c1186114760`，已推送 master。
线上部署：`dpl_4WyK3RaF3Ykq6QkMLQr8HR8moRep`，READY。
工作流验证：https://github.com/yanghaoleng/Mikey-index/actions/runs/34575697534

JoJo-Demos：发布时排除 `/apps/`、`/skills/` 和 source map；移除 15 个无引用的旧 Intermotion JS/CSS 文件。保留全部现有演示地址。构建完成后只清理不再属于本次构建的 Intermotion 资源，保留手工维护的模型、媒体和图标，防止历史构建重新累积。

单次原始上传资源从 262,431,913 字节降为约 177,137,231 字节，减少 85,294,682 字节（32.5%）。这不是账单空间即时释放量。
提交：`9fbe02c`、`74f361c`，已推送 main。
线上部署：`dpl_AVHPFhgWKqjZHX6aLVG9KsaZuru3`，READY。
所有 533 个预览文件的内容哈希和本地一致，排除目录及映射文件均未上传。

## 线上检查

- 日程接口 HTTP 200，返回非模拟的最新 21 天数据。
- jojodemos 首页、/intermotion/、/jocam/、/ti、/ququ、当前 Intermotion 主脚本均 HTTP 200。
- 被排除的开发目录资源返回 HTTP 404。
- mikeywa 首页自动请求遇到 Vercel Security Checkpoint（429）；随后通过 Safari 实际打开，确认首页、21 天日历和功能入口正常显示。未修改安全配置。
- 本地原有未提交工作保持不变，发布在独立工作目录完成。

## 统计说明

清理前面板显示 mikeywa 30.99 GB、jojodemos 9.72 GB。Vercel 用量以每日峰值等规则统计，删除记录后历史图表不会立即清零；实际结算和后续统计以控制台为准。

https://vercel.com/docs/deployment-storage

## 后续调整：每天三次自动部署

按用户后续要求，恢复通过 Vercel Deploy Hook 自动部署刷新，计划北京时间每天 08:00、14:00、20:00（UTC 00:00、06:00、12:00）。提交 `0fbb684` 已推送 master。页面优先读取实时日程接口的改动保留。上文“定时任务不部署”是此前阶段结果，现由此配置替代。

按保留部署数量和资源大小估算，清理后的账号存储目标约 5–7 GB；这不是面板实测释放量。若仍保留约 30 天历史，每天三次约产生 90 次部署，mikeywa 对应原始资源约 17.3 GB，未来总量可能回到约 20 GB 或更高，取决于额外代码推送及保留例外。

## 历史保留期设置

控制台可选 1、7、14、30 天，无 3 天选项。按此前同意的 7 天方案，已将 mikeywa 和 jojodemos 的生产、预览、失败及取消部署保留期全部设为 7 天，并通过 API 回读验证。Vercel 仍保留最近 20 个成功生产部署和线上绑定等例外版本；到期清理有后台延迟。GitHub 提交记录不受影响。
